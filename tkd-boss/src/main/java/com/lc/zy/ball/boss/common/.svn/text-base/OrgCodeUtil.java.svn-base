package com.lc.zy.ball.boss.common;

import com.lc.zy.ball.boss.common.service.AbstractCacheService;
import com.lc.zy.common.bean.CacheKeys;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Repository;

@Repository
public class OrgCodeUtil extends AbstractCacheService implements CacheKeys {

	private static Logger logger = LoggerFactory.getLogger(OrgCodeUtil.class);

    /**
     * <根据场馆id获取组织id><功能具体实现>
     *
     * @param statiumId
     * @return
     * @create：2015年9月22日 上午11:27:16
     * @author： sl
     */
    /*public String getOrgCode(String statiumId) {
        String orgCode = "";
        try {
            if (statiumId == null) {
                orgCode = "master";
            } else {
                orgCode = this.selectByPrimaryKey(StatiumDetail.class, statiumId).getOrgCode();
            }
        } catch (Exception e) {
            logger.debug("OrgCodeUtil_error", e);
        }
        return orgCode;
    }*/


    public static void main(String[] args) {
        System.out.println("hello");
    }

}
